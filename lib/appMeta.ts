// Central place for name/date strings that appear in shared chrome (nav, footer,
// FAQ, privacy, login). The display name lives here as a single constant — swapping
// it later is one edit and never touches component markup.
export const APP_NAME = "Pace";

// PACE = Practice And Correct Errors — the app's backronym / tagline.
export const APP_TAGLINE = "Practice And Correct Errors";

// Primary domain (informational; used in copy where a URL is helpful).
export const APP_DOMAIN = "pacesat.com";

// Human-readable "last updated" shown in the footer. Bump this on each release.
export const LAST_UPDATED = "August 2026";
