// Central place for name/date strings that appear in shared chrome (footer, FAQ,
// privacy page). Branding is deferred, so the display name lives here as a single
// constant — swapping it later is one edit and never touches component markup.
export const APP_NAME = "SAT Question Bank";

// Human-readable "last updated" shown in the footer. Bump this on each release.
export const LAST_UPDATED = "August 2026";
